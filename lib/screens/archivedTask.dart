

import 'package:flutter/cupertino.dart';

class archived_task extends StatelessWidget {
  const archived_task ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Archived tasks"),);
  }
}
